<%= form_for [@user, @course] do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :day_night, 'Click on box if it is a day class' %>
<%= f.check_box :day_night %>

<%= f.submit %>

<% end %>
